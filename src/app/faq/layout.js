export const metadata = {
  title: "FAQ",
  description: "Find answers to common questions about ordering, shipping, returns, and customization at The Hamper House.",
};

export default function FAQLayout({ children }) {
  return children;
}
